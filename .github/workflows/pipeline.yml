name: pipeline

# One file, because every job here answers the same question: "is this commit
# shippable?". CI and CD are separate jobs joined by `needs:`, which is simpler
# and safer than chaining two workflow files with `workflow_run` (that needs an
# extra token and is easy to get subtly wrong).
on:
  push:
    # Releases are driven by tags: pushing v1.2.0 publishes and deploys. A branch
    # push only validates, so nothing reaches users until a version is chosen.
    branches: ['**']
    tags: ['v*']
  pull_request:
  workflow_dispatch:
    inputs:
      version:
        description: >-
          Version to use instead of the tag's. Pass a LOWER version (e.g. 0.0.1)
          with draft=true to test the "update is available" path without
          publishing anything.
        required: false
        type: string
      deploy_ref:
        description: >-
          Deploy this ref to the server without publishing a release (e.g. main).
          Leave empty for a normal tag-driven deployment.
        required: false
        type: string
      draft:
        description: >-
          Publish this build as a draft release. Use together with a low version
          to test "an update is available": the build runs 0.0.1 while the feed
          still offers the published release, and nothing is added to the
          downloads page.
        required: false
        type: boolean
        default: false
      publish_android:
        description: >-
          Build and publish the Android APK for this run. Off by default; the
          repository variable PUBLISH_ANDROID turns it on permanently.
        required: false
        type: boolean
        default: false
      publish_linux:
        description: >-
          Build and publish the Linux desktop packages for this run. Off by
          default; the repository variable PUBLISH_LINUX turns it on permanently.
        required: false
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: pipeline-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: '3.12'
  NODE_VERSION: '22'
  # GHCR rejects upper-case paths. This repository is "QuickLaunchTemplate", so
  # the name must be lower-cased here - using github.repository directly works
  # until the first push and then fails in the most confusing way possible.
  IMAGE: ghcr.io/${{ github.repository_owner }}/quicklaunchtemplate

jobs:
  # ---------------------------------------------------------------------
  # 0a. The version for this build.
  #
  #      Releases are driven by tags: pushing v1.2.0 is what publishes. A branch
  #      push only runs CI, so nothing reaches users until a version is chosen.
  #
  #      Two rules protect the desktop updater, which only ever accepts a version
  #      strictly HIGHER than the running one - a repeated or lower version fails
  #      silently on the client, with nothing shown to the user:
  #        1. the tag must be v<semver>;
  #        2. it must be greater than every release already published.
  # ---------------------------------------------------------------------
  versioning:
    name: versioning
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.pick.outputs.version }}
    steps:
      # The tag guard below runs a script from the repository, so the job needs
      # the repository. (Without this the guard fails with "No such file or
      # directory" and blocks a legitimate release.)
      - uses: actions/checkout@v7

      - id: pick
        env:
          # workflow_dispatch may override, which is how the update path is
          # exercised with a version deliberately LOWER than the published one.
          INPUT_VERSION: ${{ inputs.version }}
          REF_TYPE: ${{ github.ref_type }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          if [ -n "${INPUT_VERSION}" ]; then
            version="$INPUT_VERSION"
            echo "version taken from the dispatch input"
          elif [ "${REF_TYPE}" = "tag" ]; then
            version="${REF_NAME#v}"
            echo "version taken from tag ${REF_NAME}"
          else
            version="0.0.0-dev.${{ github.run_number }}"
            echo "not a release build (branch push); using $version"
          fi

          if ! printf '%s' "$version" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?$'; then
            echo "::error::'${REF_NAME:-$version}' is not v<semver>; expected a tag like v1.2.0"
            exit 1
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"

      # Only for the numeric part: 1.0.10 must beat 1.0.9.
      - if: github.ref_type == 'tag'
        name: the tag must be newer than every published release
        env:
          GH_TOKEN: ${{ github.token }}
          NEW_VERSION: ${{ steps.pick.outputs.version }}
          REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          python3 scripts/check_version.py --version "$NEW_VERSION" --repo "$REPO"

  # ---------------------------------------------------------------------
  # 0b. Change detection, so a docs-only commit does not rebuild 3 clients.
  #
  #     It also resolves the two publication switches. The defaults encode what
  #     this project actually ships: Windows desktop and the web (the container
  #     image). Android and Linux desktop are built only when asked for, so a
  #     normal release does not spend minutes producing an APK nobody installs.
  #
  #     Turn them on permanently with repository variables, or for one run with
  #     a workflow_dispatch input.
  # ---------------------------------------------------------------------
  changes:
    name: changes
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      web: ${{ steps.filter.outputs.web }}
      deployable: ${{ steps.filter.outputs.deployable }}
      publish_android: ${{ steps.switches.outputs.publish_android }}
      publish_linux: ${{ steps.switches.outputs.publish_linux }}
    steps:
      - uses: actions/checkout@v7

      - id: switches
        env:
          # `== 'true'` matters: vars.* is a string, and "false" is truthy in an
          # expression, so a plain `vars.PUBLISH_ANDROID` would always be true.
          VAR_ANDROID: ${{ vars.PUBLISH_ANDROID == 'true' }}
          VAR_LINUX: ${{ vars.PUBLISH_LINUX == 'true' }}
          INPUT_ANDROID: ${{ inputs.publish_android }}
          INPUT_LINUX: ${{ inputs.publish_linux }}
        run: |
          android="${INPUT_ANDROID:-$VAR_ANDROID}"
          linux="${INPUT_LINUX:-$VAR_LINUX}"
          echo "publish_android=$android" >> "$GITHUB_OUTPUT"
          echo "publish_linux=$linux" >> "$GITHUB_OUTPUT"
          echo "publishing: windows=yes web=yes android=$android linux=$linux"

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            backend:
              - 'backend/**'
              - 'scripts/**'
              - 'Dockerfile'
              - 'compose.yaml'
              - '.github/workflows/**'
            web:
              - 'web/**'
              - 'desktop/**'
              - 'mobile/**'
              - '.github/workflows/**'
            # Anything whose result ends up inside the shipped image. `deploy`
            # gates on this: pushing a new image and then not smoke testing it is
            # how an unverified artifact becomes the one production pulls.
            deployable:
              - 'backend/**'
              - 'web/**'
              - 'scripts/smoke.py'
              - 'Dockerfile'
              - 'compose.yaml'
              - '.github/workflows/**'

  # ---------------------------------------------------------------------
  # 1. CI - the gates. Nothing ships unless these are green.
  # ---------------------------------------------------------------------
  verify-backend:
    name: backend / lint + unit + integration
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.backend == 'true'

    services:
      # Same major version as production: that is what turns "will the
      # migration work in production?" into a CI question.
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: quicklaunch
          POSTGRES_PASSWORD: quicklaunch
          POSTGRES_DB: quicklaunch_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U quicklaunch"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 20

    env:
      DATABASE_URL: postgresql+psycopg://quicklaunch:quicklaunch@localhost:5432/quicklaunch_test
      JWT_SECRET: ci-secret-that-is-definitely-long-enough-for-hs256
      BCRYPT_ROUNDS: '4'

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: pip
          cache-dependency-path: backend/requirements-dev.txt

      - name: install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: lint and format (ruff)
        working-directory: backend
        run: |
          ruff check .
          ruff format --check .
          # scripts/ is part of the delivery path: smoke.py is a deploy gate and
          # desktop_api_stub.py runs during desktop verification.
          ruff check ../scripts
          ruff format --check ../scripts

      - name: migrations apply to postgres 16
        working-directory: backend
        run: alembic upgrade head

      - name: migrations are reversible
        working-directory: backend
        run: |
          alembic downgrade base
          alembic upgrade head

      - name: unit tests (no database)
        working-directory: backend
        run: pytest tests/unit -q

      - name: integration tests (real postgres, real HTTP)
        working-directory: backend
        run: pytest tests/integration -q

      # The smoke-image job starts the fake payment provider with a bare `python3`
      # that has none of these dependencies, and a version that imported the app
      # package failed there with ModuleNotFoundError after the whole image had
      # been built. One second here instead of a wasted pipeline.
      - name: the fake payment provider runs without the app's dependencies
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import ast, pathlib, sys

          # Everything it imports must be stdlib or its own sibling.
          allowed = {"fake_stripe_core"}
          src = pathlib.Path("scripts/fake_stripe.py").read_text()
          imported = set()
          for node in ast.walk(ast.parse(src)):
              if isinstance(node, ast.Import):
                  imported.update(alias.name.split(".")[0] for alias in node.names)
              elif isinstance(node, ast.ImportFrom) and node.module and node.level == 0:
                  imported.add(node.module.split(".")[0])

          third_party = imported - allowed - set(sys.stdlib_module_names)
          if third_party:
              print(f"::error::fake_stripe.py imports non-stdlib modules: {sorted(third_party)}")
              sys.exit(1)
          print("fake_stripe.py imports only stdlib:", sorted(imported))
          PY

  verify-web:
    name: web / lint + types + unit + build
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.web == 'true'

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: web/package-lock.json

      - name: install dependencies
        working-directory: web
        run: npm ci --no-audit --no-fund

      - name: lint
        working-directory: web
        run: npm run lint

      - name: typecheck
        working-directory: web
        run: npm run typecheck

      - name: unit tests
        working-directory: web
        run: npm test

      - name: production build
        working-directory: web
        run: npm run build

      - name: process the built bundle
        working-directory: web
        run: |
          # The bundle must be root-relative ("." base) so the identical artifact
          # also loads from app://bundle and from the Android WebView. That comes
          # from web/.env, not from a per-job environment variable, so every job
          # cannot drift from the others.
          grep -q 'src="\./assets/' dist/index.html \
            || { echo "dist/index.html does not use relative asset paths"; cat dist/index.html; exit 1; }
          echo "bundle uses relative asset paths"

      - uses: actions/upload-artifact@v7
        with:
          name: web-dist
          path: web/dist
          retention-days: 7

  # ---------------------------------------------------------------------
  # 2. CD - build the artifact, ship it, then prove it works.
  # ---------------------------------------------------------------------
  docker:
    name: cd / build + push image
    runs-on: ubuntu-latest
    needs: [verify-backend, verify-web]
    # `always()` so a skipped verify (no changes in that area) does not block
    # the release; a *failed* verify still does, because that is a real failure.
    if: |
      always()
      && !cancelled()
      && !contains(needs.*.result, 'failure')
      && github.event_name != 'pull_request'

    permissions:
      contents: read
      packages: write

    outputs:
      digest: ${{ steps.build.outputs.digest }}
      tags: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v7

      # arm64 is emulated, which is slow; the demo ships a single-arch image.
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=sha,prefix=sha-,format=short
            type=raw,value=latest,enable={{is_default_branch}}

      - id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          load: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Keep the exact bytes we just pushed, so the smoke test below exercises
      # this build and not a re-pull that could race.
      - name: export image for the deploy stage
        run: docker save "${{ env.IMAGE }}:sha-${GITHUB_SHA::7}" -o /tmp/image.tar

      - uses: actions/upload-artifact@v7
        with:
          name: app-image
          path: /tmp/image.tar
          retention-days: 1

  # Smoke test the image the runner just built, on the runner. This is the fast
  # gate that catches a broken image before anything is shipped anywhere; the
  # separate `deploy-server` job is what actually deploys.
  smoke-image:
    name: cd / smoke the built image
    runs-on: ubuntu-latest
    needs: [changes, docker]
    # Smoke test every image that could actually be pulled. If the image was
    # built from unchanged code, `docker` still pushes a new sha- tag, and that
    # tag must not go untested just because the verify jobs were skipped.
    if: |
      always()
      && needs.docker.result == 'success'
      && (needs.changes.outputs.deployable == 'true' || github.event_name == 'workflow_dispatch')

    # compose.yaml interpolates these for every docker compose command, not just
    # the one that starts the stack - without them even `compose down` fails with
    # "required variable JWT_SECRET is missing a value".
    #
    # The STRIPE_* values point the image at a Stripe-shaped server running on
    # this runner (scripts/fake_stripe.py). That is what lets the payment path -
    # checkout over HTTP, a signed webhook, entitlement, quota lifted - be
    # exercised on every build without an account, credentials or a human with a
    # card. They are obviously fake values, and they never reach the deployment.
    env:
      JWT_SECRET: deploy-time-secret-that-is-long-enough-0001
      POSTGRES_PASSWORD: ci-postgres-password
      APP_PORT: '8000'
      STRIPE_SECRET_KEY: sk_test_fake_for_pipeline
      STRIPE_WEBHOOK_SECRET: whsec_fake_for_pipeline
      STRIPE_PRICE_PLUS: price_fake_plus
      STRIPE_PRICE_PRO: price_fake_pro
      STRIPE_API_BASE: http://host.docker.internal:12194

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - uses: actions/download-artifact@v8
        with:
          name: app-image
          path: /tmp

      # Started first: the app asks it for a checkout url during the smoke test,
      # and it delivers webhooks back to the app.
      - name: start the fake payment provider
        run: |
          set -euo pipefail
          nohup python3 scripts/fake_stripe.py \
            --host 0.0.0.0 --port 12194 \
            --webhook-target http://127.0.0.1:8000/api/billing/webhook \
            > /tmp/fake-stripe.log 2>&1 &
          for i in $(seq 1 30); do
            curl -fsS -X POST http://127.0.0.1:12194/__control/reset >/dev/null && break
            sleep 1
          done
          echo "fake provider is up:"; tail -2 /tmp/fake-stripe.log

      # The migrations service in compose.yaml runs first and the app waits for
      # it, so a broken schema change fails here - loudly - instead of surfacing
      # as 500s after the rollout. Gating is explicit rather than relying on
      # `--wait` semantics, so the log says exactly which condition was met.
      - name: start the stack
        env:
          APP_IMAGE: ${{ env.IMAGE }}:sha-${{ github.sha }}
        run: |
          set -euo pipefail
          short="${GITHUB_SHA::7}"
          docker load -i /tmp/image.tar
          docker tag "${IMAGE}:sha-${short}" "${IMAGE}:sha-${GITHUB_SHA}"
          export APP_IMAGE="${IMAGE}:sha-${GITHUB_SHA}"

          # host.docker.internal is how the container reaches the provider on the
          # runner; on Linux that needs an explicit mapping.
          export STRIPE_API_BASE="http://host.docker.internal:12194"

          docker compose -f compose.yaml up -d --wait db
          for i in $(seq 1 30); do
            state=$(docker inspect --format '{{.State.Health.Status}}' "$(docker compose ps -q db)")
            [ "$state" = "healthy" ] && break
            sleep 1
          done
          echo "db health: $state"
          [ "$state" = "healthy" ]

          echo "--- applying migrations (one-shot service) ---"
          docker compose -f compose.yaml up --exit-code-from migrate migrate

          echo "--- starting the app ---"
          docker compose -f compose.yaml up -d --wait app
          docker compose -f compose.yaml ps -a

      - name: show container state
        if: always()
        run: |
          docker compose -f compose.yaml ps -a || true
          for c in $(docker compose -f compose.yaml ps -aq); do
            echo "===== inspect $c ====="
            docker inspect --format '{{.Name}} state={{.State.Status}} health={{if .State.Health}}{{.State.Health.Status}}{{else}}none{{end}} exit={{.State.ExitCode}}' "$c"
            echo "----- last health probe output -----"
            docker inspect --format '{{range .State.Health.Log}}{{.ExitCode}}: {{.Output}}{{end}}' "$c" 2>/dev/null | tail -c 800 || true
          done
          echo "===== migrate logs ====="
          docker compose -f compose.yaml logs migrate || true
          echo "===== app logs ====="
          docker compose -f compose.yaml logs app || true

      - name: smoke test the live deployment
        run: python3 scripts/smoke.py --base-url http://127.0.0.1:8000 --health-budget-ms 1000

      - name: verify the image healthcheck agrees
        run: |
          cid="$(docker compose -f compose.yaml ps -q app)"
          # Docker checks every 10s; polling avoids racing the check interval.
          for i in $(seq 1 18); do
            state="$(docker inspect --format '{{.State.Health.Status}}' "$cid")"
            echo "container health: $state"
            [ "$state" = "healthy" ] && exit 0
            sleep 5
          done
          echo "container never reported healthy"; exit 1

      # Rollback rehearsal: redeploy the previous sha- tag and prove the service
      # comes back. Skipped on the very first successful build, when there is no
      # earlier tag to go back to. continue-on-error keeps a rehearsal failure
      # from failing an otherwise healthy deploy, but the next step reports the
      # outcome loudly rather than swallowing it.
      - name: rollback rehearsal
        id: rollback
        continue-on-error: true
        env:
          PREVIOUS_SHA: ${{ github.event.before }}
        run: |
          set -euo pipefail
          if [ -z "${PREVIOUS_SHA:-}" ] || [ "${PREVIOUS_SHA}" = "0000000000000000000000000000000000000000" ]; then
            echo "no previous commit on this ref; nothing to roll back to"; exit 0
          fi
          previous="${PREVIOUS_SHA:0:7}"
          echo "redeploying ${IMAGE}:sha-${previous}"
          docker compose -f compose.yaml down -v
          docker pull "${IMAGE}:sha-${previous}"
          export APP_IMAGE="${IMAGE}:sha-${previous}"
          docker compose -f compose.yaml up -d --wait db
          docker compose -f compose.yaml up --exit-code-from migrate migrate
          docker compose -f compose.yaml up -d --wait app
          python3 scripts/smoke.py --base-url http://127.0.0.1:8000 --skip-spa

      - name: report the rollback rehearsal
        if: always() && steps.rollback.outcome != 'skipped'
        run: |
          if [ "${{ steps.rollback.outcome }}" = "success" ]; then
            echo "::notice::rollback rehearsal passed: the previous sha- tag redeployed and served traffic"
          else
            echo "::warning::rollback rehearsal did NOT complete - check the previous sha- tag exists in GHCR"
          fi

      - name: tear down
        if: always()
        run: docker compose -f compose.yaml down -v || true

  # ---------------------------------------------------------------------
  # 3. The other two clients. Same web bundle, wrapped twice.
  #
  # Desktop packaging is split by platform on purpose: electron-builder builds a
  # Windows NSIS installer by driving Wine, and "spawn wine ENOENT" is what that
  # looks like from a Linux runner. Whatever the host OS is, it builds that OS's
  # installer - no emulation.
  # ---------------------------------------------------------------------
  desktop:
    name: desktop / package (${{ matrix.target }})
    runs-on: ${{ matrix.runner }}
    needs: [changes, verify-web, versioning]
    if: |
      always()
      && needs.verify-web.result != 'failure'
      && needs.verify-web.result != 'cancelled'
      && needs.versioning.result == 'success'
      && github.event_name != 'pull_request'

    strategy:
      fail-fast: false
      matrix:
        include:
          - target: windows
            runner: windows-latest
            script: dist:win
            artifact: desktop-windows
          # Linux is built only when the switch is on. Shipping an AppImage to
          # nobody costs a runner minute and clutters the downloads page.
          - target: linux
            runner: ubuntu-latest
            script: dist:linux
            artifact: desktop-linux
            enabled: ${{ needs.changes.outputs.publish_linux }}

    steps:
      # GitHub Actions has no supported way to skip a single matrix leg by
      # condition, so a switched-off target runs a short, successful no-op: every
      # real step below carries the same condition. A green no-op is deliberate -
      # a red "skipped" leg would make every default release look half-broken.
      - if: matrix.enabled != 'true' && matrix.target != 'windows'
        name: this target is switched off
        run: |
          echo "::notice::${{ matrix.target }} desktop packaging is off; enable it with the repository variable PUBLISH_LINUX=true"
      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        uses: actions/checkout@v7

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: |
            desktop/package-lock.json
            web/package-lock.json

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: install toolchains
        working-directory: desktop
        run: npm ci --no-audit --no-fund

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: unit tests (update version comparison)
        working-directory: desktop
        run: npm test

      # The version decides whether installed clients ever see this update:
      # electron-updater only offers a version strictly higher than the running
      # one, so "1.0.0" for every build would silently never update anyone.
      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: stamp the build version
        working-directory: desktop
        run: npm run stamp -- ${{ needs.versioning.outputs.version }}

      # The desktop shell ships the web bundle as a resource. Reuse the web
      # job's artifact when it exists, but do not depend on it: that job is
      # skipped when nothing under web/ changed, and a missing artifact would
      # otherwise fail an unrelated desktop build. (Observed exactly that in
      # run #34653287358.)
      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        uses: actions/download-artifact@v8
        continue-on-error: true
        with:
          name: web-dist
          path: web/dist

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: make sure web/dist has a relative-path bundle
        shell: bash
        env:
          # The web job builds with a relative base and that artifact is reused
          # as-is. When it is missing, this rebuild must produce the same shape,
          # or the packaged app loads /assets/... from app://bundle and shows a
          # blank window.
          VITE_WEB_BASE: './'
        run: |
          set -euo pipefail
          # The artifact may have landed one level down; normalise it.
          if [ ! -f web/dist/index.html ]; then
            found="$(find web/dist -name index.html -print -quit 2>/dev/null || true)"
            if [ -n "$found" ]; then
              mv "$(dirname "$found")" web/dist-normalised
              rm -rf web/dist
              mv web/dist-normalised web/dist
            fi
          fi
          # A bundle with absolute asset URLs cannot be loaded from app://; treat
          # it as unusable and rebuild rather than shipping a blank window.
          if [ -f web/dist/index.html ] && ! grep -q 'src="\./assets/' web/dist/index.html; then
            echo "artifact uses absolute asset paths; rebuilding with a relative base"
            rm -rf web/dist
          fi
          if [ -f web/dist/index.html ]; then
            echo "reusing the web job's artifact"; ls -la web/dist; exit 0
          fi
          echo "no usable web artifact (that job was skipped); building here"
          cd web && npm ci --no-audit --no-fund && npm run build
          test -f dist/index.html
          grep -q 'src="\./assets/' dist/index.html

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: package
        working-directory: desktop
        env:
          # Public URL the packaged app talks to by default. Empty = same origin.
          QL_API_BASE: ${{ vars.QL_API_BASE }}
          CSC_IDENTITY_AUTO_DISCOVERY: 'false'
        # `--publish never`: the publish provider is configured so electron-builder
        # writes latest.yml, but uploading is the release job's business. Without
        # this flag electron-builder tries to publish during the build and fails
        # demanding GH_TOKEN.
        run: npm run ${{ matrix.script }} -- --publish never

      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: |
            desktop/release/*.exe
            desktop/release/*.zip
            desktop/release/*.AppImage
            desktop/release/*.deb
            # latest.yml is the update feed electron-updater reads; the blockmap
            # lets it download only the changed bytes. Linux writes its own feed.
            desktop/release/latest.yml
            desktop/release/latest-linux.yml
            desktop/release/*.blockmap
          if-no-files-found: error
          retention-days: 7

      # This job runs on both Windows and Linux, and `ls -lh` is not a PowerShell
      # command - the matrix step needs an explicit shell.
      #
      # Each platform writes its own feed file: `latest.yml` for Windows and
      # `latest-linux.yml` for AppImage. Only the Windows one matters here (the
      # desktop client under test is the Windows build), but both are uploaded.
      - if: matrix.enabled == 'true' || matrix.target == 'windows'
        name: check the platform update feed was generated
        shell: bash
        working-directory: desktop
        env:
          FEED: ${{ matrix.target == 'windows' && 'latest.yml' || 'latest-linux.yml' }}
        run: |
          ls -lh release || true
          echo "--- $FEED (the update feed a client reads) ---"
          if [ -f "release/$FEED" ]; then
            cat "release/$FEED"
          else
            echo "::error::$FEED was not generated - installed clients could never update"
            exit 1
          fi

  # Packaging a desktop app is not evidence that it runs. This job installs the
  # artifact on a real Windows machine, boots it headlessly, and lets it call the
  # API from inside the renderer and read a real published update feed.
  #
  # It runs after `release` on purpose: the update path can only be tested once
  # the feed is actually published. The gates that must stop a bad build - lint,
  # unit, integration, web, docker, deploy/smoke - all run before `release`, so
  # this ordering does not weaken them.
  desktop-self-test:
    name: desktop / self-test the packaged app (windows)
    runs-on: windows-latest
    needs: [versioning, desktop, release]
    # Only where a release exists to point the updater at.
    if: always() && needs.release.result == 'success'

    steps:
      - uses: actions/checkout@v7

      - uses: actions/download-artifact@v8
        with:
          name: desktop-windows
          path: dist

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      # Read the feed from the newest PUBLISHED release that is not the one this
      # run publishes. Drafts are skipped for two reasons: an unpublished draft is
      # exactly the release this test build would be looking past, and the API the
      # real updater queries does not list drafts at all - so pointing the test at
      # one would test something the client can never see.
      - name: resolve the update feed to test against
        id: feed
        shell: bash
        run: |
          set -euo pipefail
          exclude="v${{ needs.versioning.outputs.version }}"
          echo "ignoring release $exclude (that is this build)"
          url="$(curl -fsSL -H 'Accept: application/vnd.github+json' \
            "https://api.github.com/repos/${GITHUB_REPOSITORY}/releases?per_page=30" \
            | python -c "
          import json, sys
          releases = json.load(sys.stdin)
          exclude = sys.argv[1]
          for release in releases:            # newest first
              if release['tag_name'] == exclude or release['draft']:
                  continue
              if any(a['name'] == 'latest.yml' for a in release.get('assets', [])):
                  print(release['assets'][0]['browser_download_url'].rsplit('/', 1)[0])
                  break
          " "$exclude")"
          if [ -z "$url" ]; then
            echo "::warning::no earlier release publishes latest.yml yet; leaving the updater on its default feed"
          fi
          echo "feed=$url" >> "$GITHUB_OUTPUT"
          echo "feed under test: ${url:-(none)}"

      - name: unpack the built app
        shell: pwsh
        run: |
          # The zip target is a plain archive of the packaged app directory.
          # Extracting a self-extracting installer would work too, but it
          # launches the app as a side effect - not what a CI step wants.
          Write-Host "artifact contents:"
          Get-ChildItem dist | Select-Object Name, Length | Format-Table | Out-String | Write-Host
          $zip = Get-ChildItem dist -Filter '*.zip' | Select-Object -First 1
          if (-not $zip) { throw "no desktop zip in the artifact" }
          Expand-Archive -Path $zip.FullName -DestinationPath "$PWD\app" -Force
          Get-ChildItem "$PWD\app" | Select-Object -First 10 Name

      - name: stub the API the shell will call
        shell: pwsh
        run: |
          # On Windows, a process started by a step is cleaned up when the step
          # ends unless it is truly detached. The stub must outlive this step,
          # so it is launched through WMI (Win32_Process.Create), which creates it
          # outside this process tree.
          $log = Join-Path $PWD 'stub.log'
          $cmd = "python `"$PWD\scripts\desktop_api_stub.py`" > `"$log`" 2>&1"
          $created = Invoke-CimMethod -ClassName Win32_Process -MethodName Create -Arguments @{ CommandLine = "cmd.exe /c $cmd" }
          Write-Host "stub launched, pid $($created.ProcessId)"

          for ($i = 0; $i -lt 40; $i++) {
            try { Invoke-WebRequest -Uri 'http://127.0.0.1:8123/api/health' -UseBasicParsing | Out-Null; break }
            catch { Start-Sleep -Milliseconds 500 }
          }
          $health = Invoke-WebRequest -Uri 'http://127.0.0.1:8123/api/health' -UseBasicParsing
          Write-Host "stub health: $($health.Content)"
          if (-not $health) { throw 'the stub API did not start' }

      - name: run the packaged app's self test
        timeout-minutes: 5
        shell: pwsh
        env:
          QL_API_BASE: http://127.0.0.1:8123
          # Surfaces renderer console output (CORS, CSP, load failures) in the
          # job log; without it a "blank window" is undiagnosable from CI.
          ELECTRON_ENABLE_LOGGING: '1'
          # Point the updater at a real published feed and read it, but do not
          # pull a 110 MB installer: this step proves the client can read an
          # update feed, not that it downloads one.
          QL_UPDATE_AUTODOWNLOAD: 'false'
          QL_UPDATE_FEED_URL: ${{ steps.feed.outputs.feed }}
        run: |
          # Fail early and clearly if the stub is gone, instead of reporting a
          # mysterious network error from inside the app.
          try {
            $health = Invoke-WebRequest -Uri 'http://127.0.0.1:8123/api/health' -UseBasicParsing -TimeoutSec 5
            Write-Host "stub reachable before the test: $($health.Content)"
          } catch {
            throw "the stub API is not running before the self test: $($_.Exception.Message)"
          }

          $exe = Get-ChildItem "$PWD\app" -Filter 'QuickLaunch.exe' -Recurse | Select-Object -First 1
          if (-not $exe) { throw 'QuickLaunch.exe not found after extraction' }
          Write-Host "running $($exe.FullName)"

          # A GUI process is not guaranteed to be waited on, and a frozen window
          # would hang the job forever. Start it, wait for the report file with a
          # deadline, then make sure nothing is left running.
          $report = Join-Path $PWD 'ql-self-test.json'
          Remove-Item $report -ErrorAction SilentlyContinue
          $proc = Start-Process -FilePath $exe.FullName -PassThru -NoNewWindow `
            -ArgumentList '--ql-self-test', "--ql-self-test-report=$report"
          $deadline = (Get-Date).AddSeconds(90)
          while (-not (Test-Path $report) -and (Get-Date) -lt $deadline) { Start-Sleep -Milliseconds 500 }

          $children = Get-Process -Name 'QuickLaunch' -ErrorAction SilentlyContinue
          if ($children) { $children | Stop-Process -Force -ErrorAction SilentlyContinue }
          Start-Sleep -Seconds 1

          if (-not (Test-Path $report)) { throw 'the app never reported a self-test result within 90s' }
          $json = Get-Content $report -Raw | ConvertFrom-Json
          # Depth 8 so the nested probe results survive serialisation.
          Write-Host "self test report: $($json | ConvertTo-Json -Depth 8 -Compress)"
          if (Test-Path 'stub.log') { Write-Host "--- stub log ---"; Get-Content 'stub.log' | Select-Object -Last 20 }
          if (-not $json.ok) { throw "self test failed: $($json | ConvertTo-Json -Depth 8 -Compress)" }
          Write-Host 'desktop artifact verified'

      - name: stop the stub
        if: always()
        shell: pwsh
        run: |
          Get-CimInstance Win32_Process -Filter "Name = 'python.exe'" |
            Where-Object { $_.CommandLine -like '*desktop_api_stub*' } |
            ForEach-Object { Stop-Process -Id $_.ProcessId -Force -ErrorAction SilentlyContinue }
          Write-Host 'stub stopped'

  android:
    name: android / build apk
    runs-on: ubuntu-latest
    needs: [changes, verify-web]
    # Android is off unless asked for: this project ships a Windows desktop client
    # and the web, so a normal release should not spend two runner minutes on an
    # APK nobody installs. Enable with the repository variable PUBLISH_ANDROID=true
    # or a workflow_dispatch input.
    if: |
      always()
      && needs.changes.outputs.publish_android == 'true'
      && needs.verify-web.result != 'failure'
      && needs.verify-web.result != 'cancelled'
      && github.event_name != 'pull_request'

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: npm
          cache-dependency-path: mobile/package-lock.json

      # The runner image already ships a JDK and the Android SDK; this pins them
      # so a runner image bump cannot silently change the build.
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '21'

      - uses: android-actions/setup-android@v4

      - name: install the exact platform Capacitor asks for
        run: |
          sdkmanager --install "platforms;android-36" "build-tools;36.0.0" "platform-tools"
          yes | sdkmanager --licenses > /dev/null || true

      - name: install capacitor toolchain
        working-directory: mobile
        run: npm ci --no-audit --no-fund

      # verify_apk.py is plain stdlib Python, but the runner only guarantees
      # `python3`; ask setup-python for a known interpreter instead of hoping.
      - uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: build web bundle for the WebView
        working-directory: web
        env:
          VITE_WEB_BASE: './'
          # Capacitor copies whatever `webDir` points at, so the bundle has to be
          # emitted straight into mobile/www - building to web/dist and syncing
          # fails with "Could not find the web assets directory: ./www".
          VITE_OUT_DIR: ../mobile/www
          # The APK must call a reachable server. Set the repository variable
          # QL_API_BASE to the deployed URL; localhost would only work on an
          # emulator, which is why the runtime override in the UI exists too.
          VITE_API_BASE: ${{ vars.QL_API_BASE }}
        run: |
          npm ci --no-audit --no-fund
          npm run build
          # Fail here, with a clear message, rather than inside cap sync.
          test -f ../mobile/www/index.html

      - name: generate the native project, then copy the bundle into it
        working-directory: mobile
        run: |
          if [ ! -d android ]; then npx cap add android; fi
          npx cap sync android
          test -f android/app/src/main/assets/public/index.html
          echo "cap sync ok"

      - name: build the debug apk (signed with the standard debug key)
        working-directory: mobile/android
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon assembleDebug

      - name: report apk
        working-directory: mobile
        run: ls -lh android/app/build/outputs/apk/debug/

      - name: verify the apk actually contains our app
        working-directory: mobile
        run: |
          apk="$(find android/app/build/outputs/apk/debug -name '*.apk' | head -n1)"
          python ../scripts/verify_apk.py "$apk"

      - uses: actions/upload-artifact@v7
        with:
          name: android-apk
          path: mobile/android/app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 7

  # ---------------------------------------------------------------------
  # 4. Everything a user can actually download, in one place.
  # ---------------------------------------------------------------------
  release:
    name: release / publish build
    runs-on: ubuntu-latest
    needs: [versioning, changes, docker, smoke-image, desktop, android]
    if: |
      always()
      && !cancelled()
      && !contains(needs.*.result, 'failure')
      && (github.ref_type == 'tag' || inputs.version != '')

    permissions:
      contents: write

    steps:
      # Only attach what this build actually publishes; a missing download would
      # otherwise fail the whole release.
      - if: needs.changes.outputs.publish_android == 'true'
        uses: actions/download-artifact@v8
        with:
          name: android-apk
          path: dist/android

      - uses: actions/download-artifact@v8
        with:
          name: desktop-windows
          path: dist/desktop

      - if: needs.changes.outputs.publish_linux == 'true'
        uses: actions/download-artifact@v8
        with:
          name: desktop-linux
          path: dist/linux

      - uses: actions/download-artifact@v8
        with:
          name: web-dist
          path: dist/web
        continue-on-error: true

      - name: assemble the release bundle
        run: |
          set -euo pipefail
          cd dist
          if [ -f web/index.html ]; then
            zip -qr "web-${GITHUB_SHA::7}.zip" web
            rm -rf web
          else
            echo "no static web artifact in this run (web/ unchanged); skipping that zip"
          fi
          ls -lhR .

      # electron-updater reads latest.yml out of the newest release. Without it
      # in the release, every installed client silently stops seeing updates.
      - name: check the update feed is part of this release
        run: |
          set -euo pipefail
          if [ ! -f dist/desktop/latest.yml ]; then
            echo "::error::latest.yml is missing - installed desktop clients would never update"
            exit 1
          fi
          echo "latest.yml:"; cat dist/desktop/latest.yml
          grep -q "version: ${{ needs.versioning.outputs.version }}" dist/desktop/latest.yml \
            || { echo "::error::latest.yml does not advertise version ${{ needs.versioning.outputs.version }}"; exit 1; }

      - name: summarise what is being published
        run: |
          {
            echo "### QuickLaunch ${{ needs.versioning.outputs.version }}"
            echo
            echo "| target | published |"
            echo "|---|---|"
            echo "| windows desktop | yes |"
            echo "| web + api image | yes |"
            echo "| linux desktop | ${{ needs.changes.outputs.publish_linux }} |"
            echo "| android apk | ${{ needs.changes.outputs.publish_android }} |"
          } >> "$GITHUB_STEP_SUMMARY"

      - uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ needs.versioning.outputs.version }}
          name: QuickLaunch ${{ needs.versioning.outputs.version }}
          draft: ${{ inputs.draft || false }}
          body: |
            Desktop build **${{ needs.versioning.outputs.version }}** from `${{ github.sha }}`.

            Installed Windows clients update themselves: they read `latest.yml` from this
            release, download in the background and install on one click.

            | target | artifact |
            |---|---|
            | desktop (Windows) | `QuickLaunch-Setup-${{ needs.versioning.outputs.version }}-x64.exe` installer, `QuickLaunch-${{ needs.versioning.outputs.version }}-win.zip` unpacked |
            | desktop (update feed) | `latest.yml`, `*.blockmap` - what electron-updater downloads |
            | web | served by `${{ env.IMAGE }}:sha-${{ github.sha }}` |
            ${{ needs.changes.outputs.publish_linux == 'true' && '| desktop (Linux) | `*.AppImage`, `*.deb` |' || '' }}
            ${{ needs.changes.outputs.publish_android == 'true' && '| android | `app-debug.apk` |' || '' }}

            A client only updates to a version **higher** than the one it runs, so this
            release must keep its version: retagging or lowering it stops updates.

            Linux desktop and Android are off by default; turn them on with the
            repository variables `PUBLISH_LINUX` / `PUBLISH_ANDROID`.
          files: |
            dist/desktop/*
            dist/linux/*
            dist/android/*
            dist/*.zip
          fail_on_unmatched_files: false

  # ---------------------------------------------------------------------
  # 5. Ship it to the server.
  #
  #    Runs on a tag (the release trigger) or on a manual dispatch that names a
  #    ref, so a deployment can be rehearsed without publishing a version.
  #
  #    The server builds the image from source rather than pulling the GHCR one:
  #    measured from that host, ghcr.io moves 74 B/s while a shallow git clone
  #    takes 7 seconds. A registry mirror or a domestic registry would let this
  #    pull instead - see deploy/deploy.sh for the measurements.
  #
  #    The acceptance criterion is the public URL answering, not the container
  #    starting: scripts/smoke.py runs against https://quicklaunch.luty.tech.
  # ---------------------------------------------------------------------
  deploy-server:
    name: cd / deploy to luty-server
    runs-on: ubuntu-latest
    needs: [versioning, verify-backend, verify-web]
    if: |
      always()
      && !cancelled()
      && !contains(needs.*.result, 'failure')
      && (github.ref_type == 'tag' || inputs.deploy_ref != '')
    concurrency:
      # Never let two deployments overlap on the same host.
      group: deploy-luty-server
      cancel-in-progress: false

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-python@v7
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: decide which ref to deploy
        id: ref
        env:
          INPUT_REF: ${{ inputs.deploy_ref }}
          REF_NAME: ${{ github.ref_name }}
        run: |
          set -euo pipefail
          ref="${INPUT_REF:-$REF_NAME}"
          echo "deploying ref: $ref"
          echo "ref=$ref" >> "$GITHUB_OUTPUT"

      - name: install the deploy key
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
        run: |
          set -euo pipefail
          install -m 700 -d ~/.ssh
          # Strip CR: a key pasted on Windows otherwise fails with a libcrypto error.
          printf '%s\n' "$DEPLOY_SSH_KEY" | tr -d '\r' > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key

          # Pin the host key rather than trusting whatever answers: this job ships
          # code to a machine that also runs unrelated production services.
          echo "pinning the server host key"
          for attempt in 1 2 3; do
            ssh-keyscan -t ed25519,rsa "${{ vars.DEPLOY_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null && break
            sleep 2
          done
          if ! ssh-keygen -F "${{ vars.DEPLOY_HOST }}" -f ~/.ssh/known_hosts >/dev/null; then
            echo "::error::could not obtain a host key for ${{ vars.DEPLOY_HOST }}"; exit 1
          fi
          ssh-keygen -lf <(ssh-keygen -F "${{ vars.DEPLOY_HOST }}" -f ~/.ssh/known_hosts | grep -v '^#')

      - name: verify the deploy key is accepted
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o BatchMode=yes \
              "${{ vars.DEPLOY_USER }}@${{ vars.DEPLOY_HOST }}" \
              'echo "connected as $(whoami)@$(hostname)"; docker --version'

      - name: sync server-side deploy files
        run: |
          set -euo pipefail
          target="${{ vars.DEPLOY_USER }}@${{ vars.DEPLOY_HOST }}"
          # compose.yaml and the overlay are versioned with the code; .env is NOT
          # sent and never overwritten - the secrets live only on the server.
          scp -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o BatchMode=yes \
            compose.yaml deploy/compose.server.yaml deploy/deploy.sh \
            "$target:~/quicklaunch/"

      - name: deploy
        env:
          REF: ${{ steps.ref.outputs.ref }}
        run: |
          set -euo pipefail
          ssh -i ~/.ssh/deploy_key -o IdentitiesOnly=yes -o BatchMode=yes \
              "${{ vars.DEPLOY_USER }}@${{ vars.DEPLOY_HOST }}" \
              "chmod +x ~/quicklaunch/deploy.sh && ~/quicklaunch/deploy.sh '${REF}' 3"

      # The gate that matters: the deployment is only done when the public URL
      # answers and the whole API flow works through it.
      - name: smoke test the public URL
        run: |
          python3 scripts/smoke.py \
            --base-url "https://quicklaunch.luty.tech" \
            --health-budget-ms 8000

      - name: report
        if: always()
        run: |
          {
            echo "### deployed ${{ needs.versioning.outputs.version }}"
            echo
            echo "| | |"
            echo "|---|---|"
            echo "| ref | \`${{ steps.ref.outputs.ref }}\` |"
            echo "| url | https://quicklaunch.luty.tech |"
            echo "| smoke | ${{ job.status }} |"
          } >> "$GITHUB_STEP_SUMMARY"
