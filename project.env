# 项目标识的唯一事实来源（迁移到新项目时改这里，别的地方不要写死）。
#
# 为什么要有这个文件：域名、仓库名、镜像名、服务器目录名散落在 workflow / compose /
# deploy.sh / package.json 里，共 10 处以上，而漏改的后果各不相同——漏改 `deploy.sh`
# 的仓库地址会直接部署失败（还算好的），漏改 electron-builder 的 `publish.owner`
# 则会让桌面端**自动更新静默失效**（最难发现的一种）。所以把值集中在这里，
# 需要它的地方从环境变量读，读不到的地方（JSON/YAML 不支持插值）由
# `python scripts/project_env.py check` 在每次 CI 上做一致性校验。
#
# 这个文件**不含任何密钥**，可以放心提交。密钥只存在于服务器 ~/<slug>/.env 与
# GitHub Secrets 里。
#
#   python scripts/project_env.py show          # 看解析后的全部值
#   python scripts/project_env.py check         # 校验仓库里没有漂移（CI 会跑）
#   python scripts/project_env.py bootstrap --repo owner/name --domain x.example.com --write

# 展示用名字：安装包名、Release 标题、Traefik 路由名
PROJECT_NAME=QuickLaunch
# 小写短名：GHCR 镜像名、compose 项目名、服务器上的部署目录 ~/<slug>
# GHCR 拒绝大写，所以它必须是全小写且不含点
PROJECT_SLUG=quicklaunch
# GitHub 仓库：owner/name（name 保持仓库真实大小写）
REPO_OWNER=luty4ng
REPO_NAME=QuickLaunchTemplate
# 对外域名（Traefik Host 规则 + 部署后公网冒烟的目标）
APP_DOMAIN=quicklaunch.luty.tech
# 客户端标识（迁移时一并改掉，否则与旧项目包名冲突）
DESKTOP_APP_ID=dev.quicklaunch.desktop
MOBILE_APP_ID=dev.quicklaunch.app
